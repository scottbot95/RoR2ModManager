import { FocusOnKeysDirective } from './focus-on-keys.directive';

describe('FocusOnKeysDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOnKeysDirective();
    expect(directive).toBeTruthy();
  });
});
