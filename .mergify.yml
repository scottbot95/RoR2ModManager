pull_request_rules:
  - name: automatic merge on CI success
    conditions:
      - status-success=continuous-integration/travis-ci/push
    actions:
      merge:
        method: merge
        strict: smart
      delete_head_branch:
